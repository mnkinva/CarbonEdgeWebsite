import SectionCard from '../landing/SectionCard';

export default function SectionCardExample() {
  return (
    <SectionCard title="Example Section">
      <p className="text-muted-foreground">This is example content inside a section card.</p>
    </SectionCard>
  );
}
